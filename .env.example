# Copy to .env and fill in. On Render, set DATABASE_URL in dashboard.
# For local dev, you can use SQLite fallback automatically.
# DATABASE_URL=postgresql://user:password@host:5432/dbname
FLASK_ENV=production
